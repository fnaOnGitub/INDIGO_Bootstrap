<Window x:Class="ControlCenter.UI.Views.ExplainDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="💬 Modalità Explain" 
        Height="650" 
        Width="800"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource ConsoleBackgroundBrush}"
        Foreground="{StaticResource ConsoleTextBrush}"
        ResizeMode="CanResize">
    
    <Window.Resources>
        <!-- Style per i titoli delle sezioni -->
        <Style x:Key="SectionTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{StaticResource ConsoleTextBrush}"/>
            <Setter Property="Margin" Value="0,15,0,8"/>
        </Style>
        
        <!-- Style per il testo normale -->
        <Style x:Key="NormalTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="{StaticResource ConsoleTextSecondaryBrush}"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="LineHeight" Value="22"/>
        </Style>
        
        <!-- Style per il pulsante -->
        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="45"/>
            <Setter Property="MinWidth" Value="120"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8"
                                BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.8"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="💬 Modalità Explain" 
                       FontSize="24" 
                       FontWeight="Bold" 
                       Foreground="{StaticResource LogInfoBrush}"
                       Margin="0,0,0,10"/>
            <TextBlock Text="Comprendi perché il sistema sta facendo questa azione" 
                       FontSize="13" 
                       Foreground="{StaticResource ConsoleTextSecondaryBrush}"
                       TextWrapping="Wrap"/>
        </StackPanel>
        
        <!-- Content Area -->
        <ScrollViewer Grid.Row="1" 
                      VerticalScrollBarVisibility="Auto"
                      Margin="0,0,0,20">
            <StackPanel>
                <!-- Spiegazione narrativa -->
                <TextBlock Text="📖 Spiegazione" 
                           Style="{StaticResource SectionTitleStyle}"/>
                <Border Background="{StaticResource ConsolePanelBrush}" 
                        CornerRadius="8" 
                        Padding="15"
                        Margin="0,0,0,10"
                        BorderBrush="{StaticResource ConsoleBorderBrush}"
                        BorderThickness="1">
                    <TextBlock x:Name="NarrativeTextBlock" 
                               Style="{StaticResource NormalTextStyle}"/>
                </Border>
                
                <!-- Motivazione tecnica -->
                <TextBlock Text="🔧 Motivazione tecnica" 
                           Style="{StaticResource SectionTitleStyle}"/>
                <Border Background="{StaticResource ConsolePanelBrush}" 
                        CornerRadius="8" 
                        Padding="15"
                        Margin="0,0,0,10"
                        BorderBrush="{StaticResource ConsoleBorderBrush}"
                        BorderThickness="1">
                    <TextBlock x:Name="TechnicalReasonTextBlock" 
                               Style="{StaticResource NormalTextStyle}"/>
                </Border>
                
                <!-- Dipendenze -->
                <TextBlock Text="🔗 Dipendenze" 
                           Style="{StaticResource SectionTitleStyle}"/>
                <Border Background="{StaticResource ConsolePanelBrush}" 
                        CornerRadius="8" 
                        Padding="15"
                        Margin="0,0,0,10"
                        BorderBrush="{StaticResource ConsoleBorderBrush}"
                        BorderThickness="1">
                    <TextBlock x:Name="DependenciesTextBlock" 
                               Style="{StaticResource NormalTextStyle}"/>
                </Border>
                
                <!-- Impatto -->
                <TextBlock Text="⚡ Impatto" 
                           Style="{StaticResource SectionTitleStyle}"/>
                <Border Background="{StaticResource ConsolePanelBrush}" 
                        CornerRadius="8" 
                        Padding="15"
                        Margin="0,0,0,10"
                        BorderBrush="{StaticResource ConsoleBorderBrush}"
                        BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="Se confermi:" 
                                   Foreground="{StaticResource LogDebugBrush}"
                                   FontWeight="SemiBold"
                                   Margin="0,0,0,5"/>
                        <TextBlock x:Name="ImpactConfirmTextBlock" 
                                   Style="{StaticResource NormalTextStyle}"
                                   Margin="10,0,0,10"/>
                        
                        <TextBlock Text="Se annulli:" 
                                   Foreground="{StaticResource LogErrorBrush}"
                                   FontWeight="SemiBold"
                                   Margin="0,10,0,5"/>
                        <TextBlock x:Name="ImpactCancelTextBlock" 
                                   Style="{StaticResource NormalTextStyle}"
                                   Margin="10,0,0,0"/>
                    </StackPanel>
                </Border>
                
                <!-- Alternative possibili -->
                <Expander Header="🔀 Alternative possibili" 
                          Foreground="{StaticResource ConsoleTextSecondaryBrush}"
                          Margin="0,15,0,0"
                          FontSize="14">
                    <Border Background="{StaticResource ConsolePanelBrush}" 
                            CornerRadius="8" 
                            Padding="15"
                            Margin="0,10,0,0"
                            BorderBrush="{StaticResource ConsoleBorderBrush}"
                            BorderThickness="1">
                        <TextBlock x:Name="AlternativesTextBlock" 
                                   Style="{StaticResource NormalTextStyle}"/>
                    </Border>
                </Expander>
                
                <!-- Dettagli tecnici completi (collassabile) -->
                <Expander Header="📚 Dettagli tecnici completi" 
                          Foreground="{StaticResource ConsoleTextSecondaryBrush}"
                          Margin="0,10,0,0"
                          FontSize="14">
                    <Border Background="{StaticResource ConsolePanelBrush}" 
                            CornerRadius="8" 
                            Padding="15"
                            Margin="0,10,0,0"
                            BorderBrush="{StaticResource ConsoleBorderBrush}"
                            BorderThickness="1">
                        <TextBlock x:Name="FullTechnicalDetailsTextBlock" 
                                   FontFamily="Consolas" 
                                   FontSize="11"
                                   Foreground="{StaticResource ConsoleTextSecondaryBrush}"
                                   TextWrapping="Wrap"/>
                    </Border>
                </Expander>
                
                <!-- Nota modalità explain -->
                <Border Background="{StaticResource ConsolePanelAltBrush}" 
                        CornerRadius="8" 
                        Padding="12"
                        Margin="0,20,0,0"
                        BorderBrush="{StaticResource ConsoleBorderBrush}"
                        BorderThickness="1">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="💡" 
                                   FontSize="16"
                                   Margin="0,0,10,0"/>
                        <TextBlock Text="Modalità Explain attiva - Ogni azione è spiegata e motivata" 
                                   FontSize="12"
                                   Foreground="{StaticResource LogWarnBrush}"
                                   TextWrapping="Wrap"
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
        
        <!-- Action Button -->
        <Grid Grid.Row="2">
            <Button Content="✅ Ho capito" 
                    Background="{StaticResource AccentCyanBrush}" 
                    Foreground="{StaticResource ConsoleBackgroundBrush}"
                    Style="{StaticResource ButtonStyle}"
                    HorizontalAlignment="Right"
                    Click="Ok_Click"/>
        </Grid>
    </Grid>
</Window>
