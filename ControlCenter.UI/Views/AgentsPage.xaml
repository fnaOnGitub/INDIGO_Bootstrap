<UserControl x:Class="ControlCenter.UI.Views.AgentsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ControlCenter.UI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="950">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">
            <!-- Titolo e Refresh -->
            <Grid Margin="0,0,0,20">
                <TextBlock Text="Agents" Style="{StaticResource TitleTextStyle}" VerticalAlignment="Center"/>
                <Button Content="Aggiorna (F5)" 
                       Command="{Binding RefreshAgentsCommand}"
                       HorizontalAlignment="Right"
                       Background="{StaticResource IndigoPrimaryBrush}"
                       Foreground="White"
                       Padding="15,8"
                       BorderThickness="0"
                       Cursor="Hand"/>
            </Grid>
            
            <!-- Indicatore di caricamento -->
            <ProgressBar IsIndeterminate="True" 
                        Height="4" 
                        Margin="0,0,0,20"
                        Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"
                        Foreground="{StaticResource IndigoPrimaryBrush}"/>
            
            <!-- Lista Agenti -->
            <ItemsControl ItemsSource="{Binding Agents}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,0,15">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <!-- Icona Status -->
                                <Ellipse Grid.Column="0" 
                                        Width="12" 
                                        Height="12" 
                                        VerticalAlignment="Center"
                                        Margin="0,0,15,0"
                                        Fill="LimeGreen">
                                    <Ellipse.Style>
                                        <Style TargetType="Ellipse">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsHealthy}" Value="False">
                                                    <Setter Property="Fill" Value="Red"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Ellipse.Style>
                                </Ellipse>
                                
                                <!-- Info Agente -->
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="16" Margin="0,0,0,5"/>
                                    <TextBlock Margin="0,0,0,3">
                                        <Run Text="Type: " FontWeight="SemiBold"/>
                                        <Run Text="{Binding Type}"/>
                                    </TextBlock>
                                    <TextBlock Margin="0,0,0,3">
                                        <Run Text="Port: " FontWeight="SemiBold"/>
                                        <Run Text="{Binding Port}"/>
                                    </TextBlock>
                                    <TextBlock Margin="0,0,0,3">
                                        <Run Text="Status: " FontWeight="SemiBold"/>
                                        <Run Text="{Binding Status}" Foreground="{StaticResource IndigoPrimaryBrush}"/>
                                    </TextBlock>
                                </StackPanel>
                                
                                <!-- Pulsante Dettagli -->
                                <Button Grid.Column="2" 
                                       Content="Dettagli" 
                                       Click="DetailsButton_Click"
                                       Tag="{Binding}"
                                       Background="{StaticResource IndigoOrangeBrush}"
                                       Foreground="White"
                                       Padding="15,8"
                                       BorderThickness="0"
                                       VerticalAlignment="Center"
                                       Cursor="Hand"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </ScrollViewer>
</UserControl>
