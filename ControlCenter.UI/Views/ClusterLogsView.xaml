<UserControl x:Class="ControlCenter.UI.Views.ClusterLogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Background="#1E1E2E">
    
    <UserControl.Resources>
        <Style x:Key="LogEntryTextBlockStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Foreground" Value="#CDD6F4"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Padding" Value="5,2"/>
        </Style>
        
        <Style x:Key="AgentButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#313244"/>
            <Setter Property="Foreground" Value="#CDD6F4"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#45475A"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="📊 Cluster Logs" 
                       FontSize="24" 
                       FontWeight="Bold" 
                       Foreground="#89DCEB"
                       Margin="0,0,0,5"/>
            <TextBlock Text="Log in tempo reale degli agenti del cluster" 
                       FontSize="13" 
                       Foreground="#BAC2DE"/>
        </StackPanel>
        
        <!-- Agent Selector -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Agente:" 
                       VerticalAlignment="Center"
                       Foreground="#CDD6F4"
                       Margin="0,0,10,0"/>
            
            <Button x:Name="OrchestratorButton"
                    Content="🎯 Orchestrator" 
                    Style="{StaticResource AgentButtonStyle}"
                    Click="SelectAgent_Click"
                    Tag="Orchestrator"/>
            
            <Button x:Name="IndigoAiWorker01Button"
                    Content="🤖 AI Worker" 
                    Style="{StaticResource AgentButtonStyle}"
                    Click="SelectAgent_Click"
                    Tag="IndigoAiWorker01"/>
            
            <Button x:Name="SystemButton"
                    Content="⚙️ System" 
                    Style="{StaticResource AgentButtonStyle}"
                    Click="SelectAgent_Click"
                    Tag="System"/>
        </StackPanel>
        
        <!-- Log Display -->
        <Border Grid.Row="2" 
                Background="#181825" 
                CornerRadius="8" 
                BorderThickness="1"
                BorderBrush="#313244">
            <TextBox x:Name="LogTextBox"
                     IsReadOnly="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     AcceptsReturn="True"
                     IsReadOnlyCaretVisible="True"
                     FontFamily="Consolas"
                     FontSize="12"
                     Background="#181825"
                     Foreground="#CDD6F4"
                     BorderThickness="0"
                     Padding="10"
                     MinHeight="400"/>
        </Border>
        
        <!-- Action Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    Margin="0,15,0,0">
            <Button Content="🔄 Refresh" 
                    Click="Refresh_Click"
                    Style="{StaticResource AgentButtonStyle}"
                    Background="#3B82F6"/>
            
            <Button Content="🗑️ Clear" 
                    Click="Clear_Click"
                    Style="{StaticResource AgentButtonStyle}"
                    Background="#F38BA8"/>
        </StackPanel>
    </Grid>
</UserControl>
